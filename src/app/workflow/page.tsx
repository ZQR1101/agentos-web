const nodes = [
  ["01", "Plan", "Planner 拆解任务", "完成"],
  ["02", "Approval", "检查工具权限", "人工确认"],
  ["03", "Execute", "调用检索与报告 Skill", "可重试"],
  ["04", "Review", "验证来源与质量", "通过 / 返工"],
];
export default function WorkflowPage() { return <main className="mx-auto max-w-6xl p-6 lg:p-9"><p className="text-sm font-medium text-indigo-600">STATE MACHINE</p><h1 className="mt-1 text-3xl font-semibold">调研报告工作流</h1><p className="mt-2 text-sm text-slate-500">第一版使用状态机，而不是难以调试的自由 Agent 循环。</p><div className="mt-10 flex flex-col gap-3 md:flex-row md:items-stretch">{nodes.map(([number, title, desc, status], index) => <div className="contents" key={title}><section className="min-w-0 flex-1 rounded-2xl border border-slate-200 bg-white p-5"><span className="text-xs font-medium text-indigo-600">{number}</span><h2 className="mt-3 font-semibold">{title}</h2><p className="mt-2 min-h-12 text-sm leading-6 text-slate-500">{desc}</p><span className="mt-5 inline-block rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{status}</span></section>{index < nodes.length - 1 && <div className="self-center text-center text-slate-300">→</div>}</div>)}</div><section className="mt-8 rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-5"><h2 className="font-medium">Loop 退出条件</h2><div className="mt-3 flex flex-wrap gap-2">{["Reviewer 通过", "达到最大 8 步", "用户拒绝审批", "工具连续失败 2 次", "超出 Token 预算"].map(item => <span key={item} className="rounded-lg bg-white px-3 py-2 text-sm text-slate-600 shadow-sm">{item}</span>)}</div></section></main>; }
