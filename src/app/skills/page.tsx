const skills = [["Research Report", "将目标转化为结构化调研报告", "research_search · citation_formatter", "Plan → Search → Synthesize → Review"], ["Source Review", "筛除不可靠或不相关的来源", "research_search · llm_reviewer", "Extract → Score → Keep / Reject"]];
export default function SkillsPage() { return <main className="mx-auto max-w-6xl p-6 lg:p-9"><p className="text-sm font-medium text-indigo-600">REUSABLE CAPABILITIES</p><h1 className="mt-1 text-3xl font-semibold">Skills</h1><p className="mt-2 text-sm text-slate-500">Skill 是可复用的能力包：定义目标、所需工具和固定步骤。</p><div className="mt-8 grid gap-4 md:grid-cols-2">{skills.map(([name, desc, toolList, flow]) => <section key={name} className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">{name}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{desc}</p><div className="mt-6 space-y-3 border-t border-slate-100 pt-4"><div><p className="text-xs font-medium uppercase tracking-wide text-slate-400">Tools</p><p className="mt-1 font-mono text-xs text-slate-600">{toolList}</p></div><div><p className="text-xs font-medium uppercase tracking-wide text-slate-400">Workflow</p><p className="mt-1 text-sm text-slate-700">{flow}</p></div></div></section>)}</div></main>; }
