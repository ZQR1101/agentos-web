const agents = [
  ["Planner", "拆分目标、选择工作流节点", "仅可使用任务状态与规划模型", "紫"],
  ["Executor", "执行批准后的 Skill 与工具调用", "可使用只读 MCP 工具", "蓝"],
  ["Reviewer", "检查来源、覆盖度与完成条件", "无外部工具权限", "绿"],
];
export default function AgentsPage() { return <main className="mx-auto max-w-6xl p-6 lg:p-9"><p className="text-sm font-medium text-indigo-600">MULTI-AGENT</p><h1 className="mt-1 text-3xl font-semibold">受控 Agent 协作</h1><p className="mt-2 text-sm text-slate-500">角色通过结构化任务状态交接，不使用无限制的自由对话。</p><div className="mt-8 grid gap-4 md:grid-cols-3">{agents.map(([name, duty, permission, color]) => <section key={name} className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center justify-between"><h2 className="font-semibold">{name}</h2><span className="rounded-full bg-slate-100 px-2 py-1 text-xs text-slate-500">{color}</span></div><p className="mt-4 text-sm leading-6 text-slate-600">{duty}</p><div className="mt-6 border-t border-slate-100 pt-4"><p className="text-xs font-medium uppercase tracking-wide text-slate-400">权限边界</p><p className="mt-2 text-sm text-slate-700">{permission}</p></div></section>)}</div><div className="mt-6 flex flex-wrap items-center gap-3 text-sm"><span className="rounded-lg bg-indigo-50 px-3 py-2 text-indigo-700">Planner</span><span className="text-slate-300">→</span><span className="rounded-lg bg-blue-50 px-3 py-2 text-blue-700">Executor</span><span className="text-slate-300">→</span><span className="rounded-lg bg-emerald-50 px-3 py-2 text-emerald-700">Reviewer</span><span className="text-slate-400">→ 通过 / 补充 / 失败</span></div></main>; }
