const servers = [["Research MCP", "已连接", "research_search, fetch_page", "公开网页检索"], ["Filesystem MCP", "未连接", "read_file, write_file", "本地文件操作"], ["GitHub MCP", "未连接", "search_code, create_issue", "代码库与 Issue" ]];
export default function MCPPage() { return <main className="mx-auto max-w-6xl p-6 lg:p-9"><p className="text-sm font-medium text-indigo-600">EXTERNAL INTEGRATIONS</p><h1 className="mt-1 text-3xl font-semibold">MCP 连接</h1><p className="mt-2 text-sm text-slate-500">MCP 让 Agent 以统一协议发现和调用外部工具。</p><div className="mt-8 space-y-3">{servers.map(([name, status, toolList, desc]) => <section key={name} className="rounded-xl border border-slate-200 bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-medium">{name}</h2><p className="mt-1 text-sm text-slate-500">{desc}</p></div><span className={`rounded-full px-3 py-1 text-xs ${status === "已连接" ? "bg-emerald-50 text-emerald-700" : "bg-slate-100 text-slate-600"}`}>{status}</span></div><div className="mt-4 border-t border-slate-100 pt-3"><p className="text-xs text-slate-400">暴露工具</p><p className="mt-1 font-mono text-xs text-slate-600">{toolList}</p></div></section>)}</div></main>; }
